# Build-time public value baked into the static export (frontend-architecture.md §4).
# No secrets belong here — this file is committed and this value is visible in the
# shipped JS bundle by design.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
