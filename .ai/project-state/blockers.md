# Blockers

| ID | Date | Phase | Raised by | Description | Evidence | Status | Resolution |
|----|------|-------|-----------|-------------|----------|--------|------------|
| B-001 | 2026-09-17 | build (T-009) | backend-developer (T-009), confirmed by orchestrator | Intermittent session-resolution race: `app/middleware/session_loader.py`'s `resolve_session` occasionally fails to attach `request.state.user` right after a fresh login, causing a spurious 401/403 on the immediately-following request. Not caused by T-009's new code — repros with plain login+logout only. Lives in session_loader.py / auth.py / csrf.py (T-006/T-008; final middleware order not locked until T-010a). **Escalated 2026-09-17 at T-016: now surfaces in real CI** (PostgreSQL 17 service container, GitHub Actions ubuntu-latest) — run https://github.com/ganesh-vilje/gp/actions/runs/35233692188 failed with `test_create_complaint_nul_byte_in_field_returns_422_and_creates_no_row[citizen_name] - assert 401 == 422` (1 failed, 97 passed), same signature as every local reproduction (spurious 401 on an unrelated, otherwise-correct test). This is no longer just a local annoyance — every future PR now has a real, nonzero chance of a red CI run for a reason unrelated to that PR's own change, which undermines the "green CI run" signal T-016 exists to provide. | Orchestrator ran `uv run pytest -m integration -q` 5x locally: run2 failed `test_auth_routes.py::test_unknown_route_error_shape_is_untouched`, run4 failed `test_complaints_routes.py::test_create_complaint_invalid_field_returns_422...[TC-API-011]`; both unrelated tests, consistent with a race rather than a logic bug. Developer's isolated repro: 4/80 sequential login+logout pairs got a spurious 401 despite a valid, unrevoked session row. CI reproduction: run 35233692188, job "Integration tests (Postgres)", 1/98 tests spuriously failed with 401 instead of 422. | open — priority raised | Route as a fix task to whoever owns session_loader/auth/csrf (T-006/T-008 authors) — needs the full security-regression subset per coding-guidelines.md. Given it now intermittently reddens real CI runs, recommend scheduling this as the very next task after M1's remaining infra work, ahead of new M2 feature work. |
| B-002 | 2026-09-17 | build (T-016) | orchestrator | T-016 (`.github/workflows/ci.yml`) cannot be completed: its done-condition is "green CI run on a real PR, log attached as evidence" (implementation-plan.md row T-016), which is impossible without a GitHub remote. `git remote -v` confirms no remote is configured. This is a known, documented precondition — GATE_6 Q2 states the human adds the GitHub remote before T-016 (task-status.md's own notes section, implementation-plan.md:300) — not a new discovery, but recording it formally as a blocker per the orchestrator's standard process. | `git remote -v` → empty; implementation-plan.md:300 ("the GitHub remote is added by the human before T-016, GATE_6 Q2"); task-status.md notes ("No GitHub remote exists yet ... human adds it before T-016"). | resolved | Human added remote `origin` (https://github.com/ganesh-vilje/gp.git) on 2026-09-17. `git remote -v` confirms it. T-016 can now proceed. |
| B-003 | 2026-09-17 | build (T-017) | orchestrator | T-017 (`api/Dockerfile`) is also blocked, same root cause as B-002: its done-condition is `docker build` run **in CI only** (no Docker on the build machine per GATE_6 Q1), and CI does not exist until T-016 lands. Already anticipated in task-status.md's notes ("T-017 deferred until CI exists (T-016)") from a prior invocation. | task-status.md notes section, prior invocation 2 (2026-09-11): "T-017 deferred until CI exists (T-016) — its only evidence is a Docker build in CI." | open | Resolves once T-016 is done (B-002 now resolved, unblocking the path). |
