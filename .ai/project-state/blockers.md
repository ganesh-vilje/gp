# Blockers

| ID | Date | Phase | Raised by | Description | Evidence | Status | Resolution |
|----|------|-------|-----------|-------------|----------|--------|------------|
| B-001 | 2026-09-17 | build (T-009) | backend-developer (T-009), confirmed by orchestrator | Intermittent session-resolution race: `app/middleware/session_loader.py`'s `resolve_session` occasionally fails to attach `request.state.user` right after a fresh login, causing a spurious 401/403 on the immediately-following request. Not caused by T-009's new code — repros with plain login+logout only. Lives in session_loader.py / auth.py / csrf.py (T-006/T-008; final middleware order not locked until T-010a). **Escalated 2026-09-17 at T-016: now surfaces in real CI** (PostgreSQL 17 service container, GitHub Actions ubuntu-latest) — run https://github.com/ganesh-vilje/gp/actions/runs/35233692188 failed with `test_create_complaint_nul_byte_in_field_returns_422_and_creates_no_row[citizen_name] - assert 401 == 422` (1 failed, 97 passed), same signature as every local reproduction (spurious 401 on an unrelated, otherwise-correct test). This is no longer just a local annoyance — every future PR now has a real, nonzero chance of a red CI run for a reason unrelated to that PR's own change, which undermines the "green CI run" signal T-016 exists to provide. **Escalated again 2026-09-18 at T-018: root cause is NOT concurrency-specific.** A T-018 rework agent reproduced the identical spurious-403 signature in a fully SEQUENTIAL, single-client, zero-threading repro (login → immediately POST as the same session) at ~13% per-request rate, and confirmed via direct DB query that the session row WAS correctly committed (matching `token_hash`, `revoked_at IS NULL`) at the moment `resolve_session`'s `SELECT` on a separate connection failed to see it — i.e. a genuine read-visibility race between the write connection and a fresh read connection, not a business-logic bug, and not fixed by giving concurrent test workers separate sessions (that isolation was already in place and made no difference). Orchestrator independently reproduced severity directly escalating with suite size: two consecutive `uv run pytest -m integration -q` runs (107 tests) on 2026-09-18 showed 15/107 and 3/107 failures respectively (previously ~1/98) — high variance, but the ceiling is now much worse than first tracked, consistent with more requests per run giving more chances to hit a per-request race rather than the bug itself changing. | Orchestrator ran `uv run pytest -m integration -q` 5x locally (2026-09-17): run2/run4 failed unrelated tests. CI reproduction: run 35233692188, 1/98 spurious. T-018 rework agent (2026-09-18): sequential no-threading repro, ~13% rate, confirmed via DB query that the row was already committed when the read missed it. Orchestrator (2026-09-18): 2 consecutive full integration runs, 15/107 then 3/107 failures. | open — top priority | Needs a real investigation task, not another rework loop: focus on `app/db/engine.py`'s connection pooling (pool_pre_ping/pool_recycle, isolation level, whether a stale connection is being reused before a commit is visible to it) and `app/services/auth.py`/`app/middleware/session_loader.py`'s read path. This is now confirmed as a genuine DB-read-visibility race, not a middleware-ordering or session-timing bug — do not re-attempt a fix via test isolation changes, that avenue is exhausted. Recommend this become the very next task, before further M2 feature work — it is actively degrading every task's verification signal. |
| B-002 | 2026-09-17 | build (T-016) | orchestrator | T-016 (`.github/workflows/ci.yml`) cannot be completed: its done-condition is "green CI run on a real PR, log attached as evidence" (implementation-plan.md row T-016), which is impossible without a GitHub remote. `git remote -v` confirms no remote is configured. This is a known, documented precondition — GATE_6 Q2 states the human adds the GitHub remote before T-016 (task-status.md's own notes section, implementation-plan.md:300) — not a new discovery, but recording it formally as a blocker per the orchestrator's standard process. | `git remote -v` → empty; implementation-plan.md:300 ("the GitHub remote is added by the human before T-016, GATE_6 Q2"); task-status.md notes ("No GitHub remote exists yet ... human adds it before T-016"). | resolved | Human added remote `origin` (https://github.com/ganesh-vilje/gp.git) on 2026-09-17. `git remote -v` confirms it. T-016 can now proceed. |
| B-003 | 2026-09-17 | build (T-017) | orchestrator | T-017 (`api/Dockerfile`) is also blocked, same root cause as B-002: its done-condition is `docker build` run **in CI only** (no Docker on the build machine per GATE_6 Q1), and CI does not exist until T-016 lands. Already anticipated in task-status.md's notes ("T-017 deferred until CI exists (T-016)") from a prior invocation. | task-status.md notes section, prior invocation 2 (2026-09-11): "T-017 deferred until CI exists (T-016) — its only evidence is a Docker build in CI." | resolved | T-016 done 2026-09-17 (green CI run at https://github.com/ganesh-vilje/gp/actions/runs/35233692188). T-017 unblocked. |
