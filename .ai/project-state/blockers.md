# Blockers

| ID | Date | Phase | Raised by | Description | Evidence | Status | Resolution |
|----|------|-------|-----------|-------------|----------|--------|------------|
| B-001 | 2026-09-17 | build (T-009) | backend-developer (T-009), confirmed by orchestrator | Intermittent session-resolution race: `app/middleware/session_loader.py`'s `resolve_session` occasionally fails to attach `request.state.user` right after a fresh login, causing a spurious 401/403 on the immediately-following request. Not caused by T-009's new code — repros with plain login+logout only. Lives in session_loader.py / auth.py / csrf.py (T-006/T-008; final middleware order not locked until T-010a). | Orchestrator ran `uv run pytest -m integration -q` 5x: run2 failed `test_auth_routes.py::test_unknown_route_error_shape_is_untouched`, run4 failed `test_complaints_routes.py::test_create_complaint_invalid_field_returns_422...[TC-API-011]`; both unrelated tests, consistent with a race rather than a logic bug. Developer's isolated repro: 4/80 sequential login+logout pairs got a spurious 401 despite a valid, unrevoked session row. | open | Route as a fix task to whoever owns session_loader/auth/csrf (T-006/T-008 authors) once T-010a locks final middleware order; needs the full security-regression subset per coding-guidelines.md. |
